export const metadata = {
  title: "Quran",
  description: "Read the Quran with Arabic text, translations, audio, and tafsir.",
};
export default function QuranLayout({ children }) {
  return children;
}
