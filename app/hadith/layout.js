export const metadata = {
  title: "Hadith",
  description: "Browse verified Hadith collections with Arabic text, grades, and references.",
};
export default function HadithLayout({ children }) {
  return children;
}
