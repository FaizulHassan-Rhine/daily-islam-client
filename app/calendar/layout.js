export const metadata = {
  title: "Islamic Calendar",
  description: "Hijri and Gregorian dates with significant Islamic days. Local moon sighting can shift dates.",
};
export default function CalendarLayout({ children }) {
  return children;
}
